import os
import numpy as np
from pose.extractor import PoseExtractor
from utils.config import DATASET_PATH, BASE_DIR

def build_dataset():
    print("Build mode placeholder – dataset already prepared.")
